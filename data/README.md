# Local data boundary

This folder is for private local datasets and nested storage repos.

The default local dataset path is:

```text
data/training_data
```

Everything under `data/` is ignored by the main Tinker repo except this README.
